@using NewLife.Cube
@using NewLife.GitCandy.Entity
@using NewLife;
@using NewLife.Web;
@using XCode;
@{
    var fact = ViewBag.Factory as IEntityFactory;
    var page = ViewBag.Page as Pager;

    var dic = new Dictionary<Int32, String>();
    dic.Add(1, "团队");
    dic.Add(0, "个人");
}
@await Html.PartialAsync("_Enable")
<div class="form-group">
    <label for="isTeam" class="control-label">团队：</label>
    @Html.ForDropDownList("isTeam", dic, page["isTeam"], "全部", true)
</div>
@*@await Html.PartialAsync("_SelectDepartment", "departmentId")*@
@await Html.PartialAsync("_DateRange")
